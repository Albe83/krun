---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ansibletasks.tasks.krun.io
spec:
  group: tasks.krun.io
  names:
    kind: AnsibleTask
    listKind: AnsibleTaskList
    plural: ansibletasks
    singular: ansibletask
    shortNames:
    - atask
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AnsibleTask is the Schema for the ansibletasks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the reusable Ansible task semantics.
            properties:
              action:
                description: Task action to execute (typically a fully qualified Ansible module name).
                minLength: 1
                type: string
              args:
                description: Arguments passed to the selected task action.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              become:
                description: Requests privilege escalation for this task.
                type: boolean
              changedWhen:
                description: Expression that overrides changed status evaluation.
                minLength: 1
                type: string
              checkMode:
                description: Forces or disables check mode for this task.
                type: boolean
              delay:
                description: Number of seconds to wait between retries when used with until.
                minimum: 0
                type: integer
              failedWhen:
                description: Expression that overrides failure status evaluation.
                minLength: 1
                type: string
              ignoreErrors:
                description: Continue role execution when this task fails.
                type: boolean
              loop:
                description: Loop input consumed by the task.
                items:
                  type: string
                type: array
              loopControl:
                description: Advanced control knobs for task loops.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              notify:
                description: List of handlers to notify when the task reports changed.
                items:
                  minLength: 1
                  type: string
                type: array
              register:
                description: Variable name used to register action results.
                minLength: 1
                type: string
              retries:
                description: Number of retries when used with until.
                minimum: 1
                type: integer
              tags:
                description: Optional Ansible tags attached to this task.
                items:
                  minLength: 1
                  type: string
                type: array
              until:
                description: Retry condition evaluated after task execution.
                minLength: 1
                type: string
              when:
                description: Conditional expression evaluated by Ansible before executing the task.
                minLength: 1
                type: string
            required:
            - action
            type: object
          status:
            description: Status contains observed information about the task resource.
            properties:
              conditions:
                description: Standard status conditions for this resource.
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      type: string
                    status:
                      pattern: ^(True|False|Unknown)$
                      type: string
                    type:
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Last reconciliation message.
                type: string
              observedGeneration:
                description: Most recent spec generation observed by the operator.
                format: int64
                minimum: 0
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
